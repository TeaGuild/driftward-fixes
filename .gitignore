/build/
/.gradle/
/run/
/default_data/
# Vendored mod jars are local-only, except tgbridge which we track so the chat
# relay builds without a manual download (it's not on a usable maven; see build.gradle).
/libs/*
!/libs/tgbridge.jar
*.classpath
*.project
.idea/
*.iml

# editor swap files
*.kate-swp
*.swp
